_base_: vespernet_default.yaml

data:
  mode: source_low_only
  source_low_dir: data/LOL/our485/low
  source_high_dir: null
  target_low_dir: null
  file_list: null

train:
  batch_size: 4
  epochs: 100
  lr: 0.0001
  save_dir: runs/stage_r
  save_name: lumi_rest_ckpt.pth
  save_interval: 20
  mask_prob: 0.2
  full_input_prob: 0.5
  lambda_grad: 0.1
  lambda_color: 0.5
  lambda_delta: 0.15
